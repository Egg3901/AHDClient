[package]
name = "tauri-plugin-briefing-widgets"
version = "0.1.0"
edition = "2021"
links = "tauri-plugin-briefing-widgets"
publish = false

[dependencies]
tauri = "2"

[build-dependencies]
tauri-plugin = { version = "2", features = ["build"] }
